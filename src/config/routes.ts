export const routes = {
  home: "/",
  company: "/company",
  aboutLegacy: "/about",
  collections: "/collections",
  collectionsDetailPattern: "/collections/:slug",
  collectionDetail: (slug: string) => `/collections/${slug}`,
  products: "/products",
  productsLegacy: "/catalog",
  productsDetailPattern: "/products/:slug",
  productDetail: (slug: string) => `/products/${slug}`,
  productsDetailLegacyPattern: "/catalog/:slug",
  productDetailLegacy: (slug: string) => `/catalog/${slug}`,
  projects: "/projects",
  projectDetailPattern: "/projects/:slug",
  projectDetail: (slug: string) => `/projects/${slug}`,
  downloads: "/downloads",
  downloadsCategoryPattern: "/downloads/:category",
  downloadsCategory: (category: string) => `/downloads/${category}`,
  downloadsLegacy: "/for-architects",
  forObjects: "/for-objects",
  forObjectsLegacy: "/custom",
  faq: "/faq",
  contacts: "/contacts",
  requestProject: "/request-project",
  materials: "/materials",
  textureDetailPattern: "/texture/:slug",
  textureDetail: (slug: string) => `/texture/${slug}`,
  materialsDetailLegacyPattern: "/materials/:slug",
  materialsDetailLegacy: (slug: string) => `/materials/${slug}`,
  privacy: "/privacy",
  cookies: "/cookies",
  consent: "/consent",
  terms: "/terms",
} as const;

export const legacyToCanonical = {
  "/catalog": routes.products,
  "/about": routes.company,
  "/for-architects": routes.downloads,
  "/custom": routes.forObjects,
} as const;
